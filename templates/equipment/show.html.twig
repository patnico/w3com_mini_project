{% extends 'base.html.twig' %}

{% block title %}Equipement{% endblock %}

{% block body %}
    <div class="container">

        <h1>Equipement</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Nom court</th>
                <td>{{ equipment.shortName }}</td>
            </tr>
            <tr>
                <th>Nom long</th>
                <td>{{ equipment.longName }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ equipment.weight }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('equipment_index') }}" class="btn btn-light">Retour à la liste</a>
        <a href="{{ path('equipment_edit', {'id': equipment.id}) }}" class="btn btn-light">Modifier</a>

    </div>
{% endblock %}
